# Telegram
# Required for real deployments. You can deploy without it, but the webhook won't send messages.
TELEGRAM_BOT_TOKEN=

# Recommended for production: validates Telegram webhook requests (X-Telegram-Bot-Api-Secret-Token)
TELEGRAM_SECRET_TOKEN=

# AWS
AWS_REGION=eu-west-1
# AWS_ACCOUNT=123456789012

# Catalog artifacts (S3)
# You can upload artifacts with:
#   CATALOG_INPUT_FILE=../../data/<merchant>/shopify-products.json pnpm index:run
CATALOG_BUCKET=
CATALOG_PREFIX=merchant/

# Optional Bedrock models (only used if your code calls Bedrock)
TITAN_EMBED_MODEL_ID=amazon.titan-embed-text-v1
CLAUDE_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0


